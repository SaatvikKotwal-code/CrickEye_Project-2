/**
 * Optional override only. Prefer backend/.env + /api/public-config (see readme).
 * Copy to config.js and load before app.js if you need a manual override.
 */
window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
